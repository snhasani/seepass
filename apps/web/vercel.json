{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=assistant-ui-starter && cd apps/web && convex deploy && pnpm run db:seed",
  "framework": "nextjs"
}
